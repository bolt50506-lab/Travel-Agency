'use client';

import AdminPlaceholder from '@/components/admin/admin-placeholder';

export default function AdminSettingsPage() {
  return <AdminPlaceholder title="Settings" description="Configure platform-wide settings including currency, language, and provider defaults." />;
}
